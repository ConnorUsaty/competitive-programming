// g++-std=c++11-O2-Wall test.cpp-o test

#include <algorithm>
#include <bitset>
#include <cassert>
#include <chrono>
#include <cmath>
#include <deque>
#include <iomanip>
#include <iostream>
#include <map>
#include <queue>
#include <random>
#include <set>
#include <string>
#include <vector>
typedef long long ll;
typedef long double ld;

using namespace std; 

// ios::sync_with_stdio(0); 
// cin.tie(0);


void solve() {
    // minimize difference between max and min
    // we care about min, and before min, have to decrease before min to increase min
    // we care about max, and after max, have to increase after max to decrease max
    int n, mxi = 0, mni = 0;
    cin >> n;
    vector<int> a(n);
    cin >> a[0];
    return;
}


int main() { 
    int t;
    cin >> t;

    while (t--) {
        solve();
    }

    return 0;
}
